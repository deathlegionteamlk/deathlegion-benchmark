from setuptools import setup, find_packages

setup(
    name="deathlegion_benchmark",
    version="0.1.0",
    description="Deathlegion Benchmark — The Hardest Coding Benchmark in Existence",
    packages=find_packages(),
    python_requires=">=3.10",
)